## Round {N} — {YYYY-MM-DD}

- proposals: {P} (acted {A}) · actors: {X} · critics/candidate: {Y}
- outcomes: {candidateDir} {score} [{verdicts} verdicts, {accepts} accept/{rejects} reject] ; ...
- selected: {candidateDir} ({score})      ← 没有就写 `none — champion unchanged`
- needsHuman: {true|false} ({原因；无则 —})

<!-- regressionFailed 时在 selected 行尾加：⚠ regression FAILED -->
<!-- L2/L3 动作追加行：criteria change r{N}: +<标准>（用户签收）等 -->